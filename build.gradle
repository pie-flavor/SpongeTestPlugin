group 'flavor.pie'
version '1.0'

apply plugin: 'java'

repositories {
    mavenCentral()
    maven {
        url 'https://jitpack.io'
    }
}

dependencies {
    compile 'com.github.pie-flavor:SpongeAPI:feature~more-arguments-SNAPSHOT'
}
