group 'flavor.pie'
version '1.0'

apply plugin: 'java'

repositories {
    mavenCentral()
    jcenter()
    maven {
        url 'https://jitpack.io'
    }
    maven {
        url 'https://oss.sonatype.org/content/groups/public/'
    }
}

dependencies {
    compile 'com.github.pie-flavor:SpongeAPI:feature~more-arguments-SNAPSHOT'
}
